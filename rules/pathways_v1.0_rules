# if sql query returns >0 results, fail_msg will be printed 
rule_name, fail_msg, rule_sql
'no self paths', 'found a path where from_stop_id is same as to_stop_id', 'SELECT * from TABLE where from_stop_id = to_stop_id'
